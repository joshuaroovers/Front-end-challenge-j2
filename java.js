partij = {"A" : 0, "B" : 0, "C" : 0, "D" : 0}

voorbeeld = "A D";
x = voorbeeld;

function eens(x)
{
    y = x.split(" ");
    z = 0;
    foreach(y)
    {
        z++
        partij[z]++;
    }
}
eens();

function eens(x)
{
    y = x.split(" ");
    z = 0;
    foreach(y)
    {
        z++
        partij[z]++;
    }
}
eens();

function oneens(x)
{
    y = x.split(" ");
    z = 0;
    foreach(y)
    {
        z++
        partij[z]++;
    }
}
oneens();